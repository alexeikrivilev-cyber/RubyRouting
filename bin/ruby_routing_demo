#!/usr/bin/env ruby
# frozen_string_literal: true

require "json"
$LOAD_PATH.unshift File.expand_path("../lib", __dir__)
require "ruby_routing"

puts JSON.pretty_generate(RubyRouting::Demo::Scenario.case_run.fetch(:report))
